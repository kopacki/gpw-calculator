import * as React from "react";
import { render } from "react-dom";

function App() {
  return <h1>GPW Calculator</h1>;
}

const rootElement = document.getElementById("root");
render(<App />, rootElement);
